#ifndef ENUMCLASSES_H
#define ENUMCLASSES_H

enum CameraView : int { MONO, STEREO };
enum CameraType : int { IP = 0, WEB = 1 };
enum ProcessStatus : int { OFF = 0, ON = 1};
enum CalibrationFileType : int {XML = 0, YML = 1};

#endif // ENUMCLASSES_H
